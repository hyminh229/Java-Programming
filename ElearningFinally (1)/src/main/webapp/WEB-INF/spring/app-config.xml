<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Kích hoạt quét component để tìm @Controller, @Service, v.v. -->
    <context:component-scan base-package="uth.edu.TrinhHoangChuong" />

    <!-- Cấu hình ViewResolver để tìm JSP files -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- Cấu hình để phục vụ tài nguyên tĩnh (CSS, JS, Images) -->
    <mvc:resources mapping="/resources/**" location="/resources/" />
    
    <!-- Đảm bảo static resources được serve -->
    <mvc:default-servlet-handler />
    <mvc:annotation-driven />

</beans>

